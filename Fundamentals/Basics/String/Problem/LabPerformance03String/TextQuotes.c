#include<stdio.h>
#include<string.h>
char sentence[100001];
int main(){
scanf("%[^\n]",sentence);
int freq[1000]={0};
for(int i=0;sentence[i]!='\0';i++){
    if(freq[(unsigned char[i])]==")
}


    return 0;
}